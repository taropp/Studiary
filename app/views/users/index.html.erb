<% provide(:title, "Users")%>
<div class="pages">
  <%= paginate @users %>
</div>
<div>
  <ul class="users">
    <% @users.each do |user| %>
      <li>
        <%= gravatar_for user, size: 50 %>
        <%= link_to user.name, user %>
      </li>
    <% end %>
  </ul>
</div>